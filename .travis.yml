language: python

python:
    - "3.6"
    
install:
  - pip install -r requirements.txt
  - pip install codecov
  - pip install coveralls
  - pip install coverage
  - pip install nose

script:
  - coverage erase
  - nosetests --with-coverage
  - coverage report -m

after_success:
  - codecov
  - coveralls